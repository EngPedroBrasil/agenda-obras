{
  "name": "agenda-obras",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/unit/*.test.js"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
