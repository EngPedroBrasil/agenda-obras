{
  "name": "agenda-obras",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node --test netlify/functions/lib/*.test.js netlify/functions/*.test.js"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
